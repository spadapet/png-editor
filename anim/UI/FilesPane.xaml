<Grid
    x:Class="anim.FilesPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:anim"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="200">

    <Grid.Resources>
        <DataTemplate x:Key="ProjectFolderTemplate" x:DataType="local:ProjectFolderVM">
            <Grid Background="Salmon" ToolTipService.ToolTip="{x:Bind FullName}">
                <TextBlock Text="{x:Bind DisplayName}" />
            </Grid>
        </DataTemplate>
        <Style x:Key="ProjectFolderListStyle" TargetType="ItemsControl">
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="ItemTemplate" Value="{StaticResource ProjectFolderTemplate}" />
        </Style>
    </Grid.Resources>

    <StackPanel>
        <TextBlock Text="Explorer" Style="{StaticResource PaneHeaderStyle}" />
        <ItemsControl ItemsSource="{x:Bind State.ProjectFolders}" Style="{StaticResource ProjectFolderListStyle}" />
    </StackPanel>
</Grid>
